<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <RootNamespace>Nudge.App</RootNamespace>
    <!-- The deliverable is a normal Windows executable called Nudge.exe. -->
    <AssemblyName>Nudge</AssemblyName>
    <ApplicationHighDpiMode>PerMonitorV2</ApplicationHighDpiMode>
    <!-- Sets the compiled exe's icon (Explorer, taskbar, Alt-Tab). -->
    <ApplicationIcon>Assets\nudge.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <!-- The UI depends on Core for its contracts, and on Vpx/Data/Library only to register their
         services - nothing in Nudge.App talks to a file, a database, or the registry directly. -->
    <ProjectReference Include="..\Nudge.Core\Nudge.Core.csproj" />
    <ProjectReference Include="..\Nudge.Vpx\Nudge.Vpx.csproj" />
    <ProjectReference Include="..\Nudge.Data\Nudge.Data.csproj" />
    <ProjectReference Include="..\Nudge.Library\Nudge.Library.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Also included as a WPF resource so Window.Icon can reference it via pack URI at runtime. -->
    <Resource Include="Assets\nudge.ico" />
    <!-- The disc mark, shown on the setup screen and used to build nudge.ico. -->
    <Resource Include="Assets\Nudge.png" />
    <!-- The library screen's header wordmark. -->
    <Resource Include="Assets\Library.png" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.0" />
    <PackageReference Include="Serilog" Version="4.2.0" />
    <PackageReference Include="Serilog.Extensions.Hosting" Version="9.0.0" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.0.0" />
    <PackageReference Include="Serilog.Sinks.File" Version="6.0.0" />
  </ItemGroup>

</Project>
